# Changelog

## 1.0.0
- Makes every registered Ball use the guaranteed-capture path.
- Removes custom Ball attempt callbacks so they cannot bypass the guarantee.
- Includes stock and mod-added Ball registry entries.
